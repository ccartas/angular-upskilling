import { ForbiddenInputDirective } from './forbidden-input.directive';

describe('ForbiddenInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenInputDirective();
    expect(directive).toBeTruthy();
  });
});
